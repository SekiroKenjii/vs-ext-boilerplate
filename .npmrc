# pnpm configuration
# Automatically install peer dependencies declared by packages.
auto-install-peers=true
# Don't fail the install on unmet optional peer ranges (keeps DX smooth).
strict-peer-dependencies=false
# Enforce the Node engine range declared in package.json.
engine-strict=true
# Deterministic, reproducible installs in CI.
prefer-frozen-lockfile=true
