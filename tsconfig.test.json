{
  // Unit tests (Vitest) + the source they exercise.
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022"],
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "test/unit/**/*.ts", "test/mocks/**/*.ts"],
  "exclude": ["src/ui/webview/client/**"]
}
